import React from 'react'
import ReactDOM from 'react-dom'
import Graph from './graph'

class App extends React.Component {
  render () {
  }
}

ReactDOM.render(<Graph />, document.getElementById('react-app'))
